{
  "name": "catalogo-decor-android",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.8.0"
  },
  "scripts": {
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  }
}
